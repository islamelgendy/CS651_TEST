# CS651_TEST
This is a simple web application consists of only one ASP.NET web page.
This Web application prompts the user to input an integer n in a TextBox, inputTextBox, and submit it. After the text is submitted, the Web server generates n random integers and responds with an XHTML page containing a ListBox, outputListBox, showing the list of generated numbers and a TextBox, rangeTextBox, showing the range of the generated numbers in the form "min – max".
